/**
 * 业务数据常用枚举类型
 */

// 发布状态
export enum EPublishState {
  Pending = 'pending',
  Passed = 'passed',
  Rejected = 'rejected',
  Deleted = 'deleted',
  Locked = 'locked'
}

// 公开状态
export enum EPublicState {
  
}